import 'package:flutter/material.dart';
class translator_features extends StatelessWidget {
  const translator_features({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: Text("Ai Translator"),),
      body: ListView(children: [],),
    );
  }
}
